<UserControl x:Class="AccommodationApplication.Controls.SimplePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccommodationApplication.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
            <PasswordBox PasswordChanged="PasswordBox_OnPasswordChanged" Name="PasswordBox"
                         Padding="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SimplePasswordBox}}, Path=Padding}"
                         Margin="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SimplePasswordBox}}, Path=Margin}"
                         BorderBrush="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:SimplePasswordBox}}, Path=BorderBrush}"/>
    </Grid>
</UserControl>
