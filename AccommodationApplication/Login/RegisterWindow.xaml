<Window x:Class="AccommodationApplication.Login.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AccommodationApplication.Login"
        xmlns:vm="clr-namespace:AccommodationApplication.ViewModels"
        xmlns:controls="clr-namespace:AccommodationApplication.Controls"
        mc:Ignorable="d"
        Title="Nowy użytkownik" Height="300" Width="300" WindowStyle="None"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/LoginStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Window.DataContext>
        <vm:RegiserNewUserViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <Label Style="{StaticResource TitleLabel}"
                   Content="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RegisterWindow}}, Path=Title}"/>
            <Button Style="{StaticResource CloseButtonStyle}" Command="{Binding CloseCommand}" />
        </DockPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Login"/>
            <TextBlock Text="Hasło" Grid.Row="1" Grid.Column="0"/>
            <TextBlock Text="Potwierdź hasło" Grid.Column="0" Grid.Row="2"/>
            <TextBlock Text="Email" Grid.Column="0" Grid.Row="3"/>
            <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Login, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
            <controls:SimplePasswordBox Grid.Row="1" Grid.Column="1" Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
            <controls:SimplePasswordBox Grid.Column="1" Grid.Row="2" Password="{Binding Password2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
            <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
        </Grid>
        <DockPanel Grid.Row="2" Margin="0,10,0,10">
            <Button Content="Zarejestruj" Style="{StaticResource LoginButtonStyle}"
                    Command="{Binding RegisterCommand}"/>
        </DockPanel>
    </Grid>
</Window>
