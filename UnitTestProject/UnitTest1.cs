using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Moq;
using System.Data.Entity;
using AccommodationDataAccess.Domain;
using AccommodationDataAccess.Model;
using AccommodationApplication.ViewModels;

namespace UnitTestProject
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void Check_user_adding()
        {
            //var mockSet = new Mock<DbSet<Offer>>();

            //var mockContext = new Mock<AccommodationContext>();
            //mockContext.Setup(m => m.Offers).Returns(mockSet.Object);

            //var offerViewModel = new AddNewOfferViewModel();

            //offerViewModel.AccommodationName = "nazwa";
            //offerViewModel.AvailiableVacanciesNumber = "1";
            //offerViewModel.City = "miasto";
            //offerViewModel.Description = "opis";
            //offerViewModel.EndDate = DateTime.Now;
            //offerViewModel.StartDate = DateTime.Now;
            //offerViewModel.LocalNumber = "1";
            //offerViewModel.PostalCode = "11-111";
            //offerViewModel.Price = "11";
            //offerViewModel.Street = "11";

            //offerViewModel.Add();

            //using (var context = new AccommodationContext())
            //{
            //}



        }
    }
}
